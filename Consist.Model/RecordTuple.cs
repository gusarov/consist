using System;

namespace Consist.Model
{
	/*
	public class RecordTuple
	{
		public MetadataContainer Container { get; set; }
		public Record Record { get; set; }

		public RecordTuple(MetadataContainer container, Record record)
		{
			Container = container;
			Record = record;
		}

		public RecordTuple(Record record)
		{
			if (record == null)
			{
				throw new Exception();
			}
			Container = record.Container;
			Record = record;
		}

		public static implicit operator RecordTuple(Record rec)
		{
			return new RecordTuple(rec);
		}
	}
	*/
}
